# Optional OpenRouter credentials for live bot responses in the browser demo.
# Copy to `.env.local` for local dev — never commit real keys.

# Visitor runtime key (paste in UI is preferred for the public demo)
VITE_OPENROUTER_KEY=

# Optional model override (defaults to openrouter/auto-beta)
VITE_MODEL=openrouter/auto-beta
